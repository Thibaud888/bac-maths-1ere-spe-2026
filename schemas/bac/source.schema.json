{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bac/source.schema.json",
  "title": "Source officielle",
  "description": "Référence officielle (education.gouv.fr, éduscol, Bulletin officiel) citée par une donnée de la page « Le bac, mode d'emploi ». Aucun chiffre de content/bac/ ne circule sans au moins une source.",
  "type": "object",
  "required": ["id", "label", "publisher", "url", "consultedOn"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^s-[a-z0-9-]+$",
      "description": "Identifiant unique. Format : s-<slug>. Ex : s-calcul-note"
    },
    "label": {
      "type": "string",
      "minLength": 3,
      "maxLength": 160,
      "description": "Titre de la page ou du texte, tel qu'il est publié."
    },
    "publisher": {
      "type": "string",
      "enum": ["education.gouv.fr", "éduscol", "Bulletin officiel"],
      "description": "Qui publie le texte."
    },
    "url": {
      "type": "string",
      "pattern": "^https://",
      "description": "Adresse de la page officielle."
    },
    "consultedOn": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Date de consultation (AAAA-MM-JJ)."
    },
    "note": {
      "type": "string",
      "maxLength": 240,
      "description": "Précision facultative : référence du BO, date de parution."
    }
  }
}
