{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "grand-oral/critere.schema.json",
  "title": "Critère valorisé par le jury du grand oral",
  "description": "Ce que le texte officiel dit que le jury valorise. Sert à la page « L'épreuve » et à l'auto-évaluation de l'oral blanc. Aucun point de barème ici : le texte officiel ne répartit pas les 20 points entre ces critères.",
  "type": "object",
  "required": ["id", "label", "aide", "sources"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^gc-[a-z0-9-]+$",
      "description": "Identifiant unique. Format : gc-<slug>. Ex : gc-connaissances"
    },
    "label": {
      "type": "string",
      "minLength": 3,
      "maxLength": 80,
      "description": "Le critère, dans les mots du texte officiel."
    },
    "aide": {
      "type": "string",
      "minLength": 10,
      "maxLength": 200,
      "description": "La question à se poser après un oral blanc (conseil, pas texte officiel)."
    },
    "order": {
      "type": "integer",
      "minimum": 0
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "pattern": "^s-[a-z0-9-]+$" }
    }
  }
}
